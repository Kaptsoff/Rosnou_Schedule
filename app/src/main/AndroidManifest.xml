<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Расписание занятий"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EducatorsActivity"
            android:exported="true"
            android:label="Преподаватели"/>

        <activity
            android:name=".DisciplinsActivity"
            android:exported="true"
            android:label="Дисциплины"/>

        <activity
            android:name=".GroupActivity"
            android:exported="true"
            android:label="Группы"/>

        <activity
            android:name=".BellScheduleActivity"
            android:exported="true"
            android:label="Расписание звонков"/>

        <activity
            android:name=".RoomsActivity"
            android:exported="true"
            android:label="Аудитории"/>

        <activity
            android:name=".WeeklyScheduleActivity"
            android:exported="true"
            android:label="Аудитории"/>

    </application>
</manifest>